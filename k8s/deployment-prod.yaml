apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-app
  namespace: prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hello-app
      env: prod
  template:
    metadata:
      labels:
        app: hello-app
        env: prod
    spec:
      containers:
        - name: hello-app
          image: DOCKER_HUB_USERNAME/hello-app:prod
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          env:
            - name: APP_ENV
              value: prod
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: hello-app
  namespace: prod
spec:
  selector:
    app: hello-app
    env: prod
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello-app
  namespace: prod
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
    - host: hello.prod.cluster.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hello-app
                port:
                  number: 80
